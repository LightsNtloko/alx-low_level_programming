#include "main.h"
#include <stdio.h>

/**
 * main - Program that prints my name
 * @argc: number of arguments to be counted
 * @argv: the array of the arguments
 *
 * Return: Always 0 (Success).
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
