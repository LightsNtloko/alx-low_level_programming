#include "main.h"

/**
 * _isdigit - checks if a character
 * is a digit
 * @k: The character to be checked.
 *
 * Return: 1 if k is a digit
 * 0 otherwise.
 */
int _isdigit(int k)
{
	if (k >= 48 && k <= 57)
		
		return (1);
	else
		return (0);
}
