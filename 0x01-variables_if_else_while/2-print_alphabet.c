#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - Execute alphabets to lower case a-z
 * Return: Always 0 (Success)
 */
int main(void)
{
	char i = 'a';

	while (k <= 'z')
	{
		putchar(i);
		i++;
	}
	putchar("\n");
	return (0);
}
