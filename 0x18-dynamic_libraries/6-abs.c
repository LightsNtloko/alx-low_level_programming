#include "main.h"

/**
 * _abs - computes the absolute value
 * of an integer.
 * @l: The integer to compute
 * the absolute value for.
 *
 * Return: The absolute value of the integer.
 */
int _abs(int l)
{
	if (l >= 0)
	{
		return (l);
	}
	else
	{
		return (-l);
	}
}
