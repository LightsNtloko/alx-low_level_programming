#include <stdio.h>

/**
 * mul - The function that multipy two integers
 * @a: First integer
 * @b: Second integer
 *
 * Return: The product of two integers
 */
int mul(int a, int b)
{
	return (a * b);
}
