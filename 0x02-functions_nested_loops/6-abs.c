#include "main.h"

/**
 * _abs - computes the absolute
 * value of an integer.
 * @k: The integer to compute the 
 * absolute value for.
 *
 * Return: The absolute value of 
 * the integer.
 */
int _abs(int k)
{
	if (k >= 0)
	{
		return (k);
	}
	else
	{
		return (-k);
	}
}
